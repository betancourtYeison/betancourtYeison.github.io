name: Deploy to GitHub Pages

# Builds the site and deploys it to GitHub Pages via the official Pages
# actions. Runs on push to `master`, the production branch: publish by
# merging a develop -> master PR. Day-to-day work lands on `develop` first
# (validated by ci.yml). Requires the repo setting Settings → Pages →
# Source = "GitHub Actions" (already configured).

on:
  push:
    branches: [master]
  workflow_dispatch:

# Least-privilege permissions the Pages deployment needs.
permissions:
  contents: read
  pages: write
  id-token: write

# Only one Pages deployment at a time; newer pushes supersede in-flight runs.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build
        # Optional GitHub/Medium data fetch (see fetch.js). These resolve to
        # empty when unset, and the build falls back to portfolio.js data.
        env:
          USE_GITHUB_DATA: ${{ vars.USE_GITHUB_DATA }}
          GITHUB_USERNAME: ${{ vars.GH_USERNAME }}
          VITE_GITHUB_TOKEN: ${{ secrets.VITE_GITHUB_TOKEN }}
          MEDIUM_USERNAME: ${{ vars.MEDIUM_USERNAME }}

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
